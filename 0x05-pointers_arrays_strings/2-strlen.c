#include <stdio.h>
/**
  *_strlen -return length of string
  *@str: the string lenth to check
  *Return: return the length
  */

size_t  _strlen(const char *str)
{
size_t length = 0;

while
(*str++);
length++;
return (length);

}
