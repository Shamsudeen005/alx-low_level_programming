#include "main.h"
/**
  *_isupper -To chech for the uppercase letter
  *@c:The character to be checked
  *Return: 1 if c is character else return 0
  */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
return (0);
}
